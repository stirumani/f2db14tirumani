extends layout

block content
  h1= title
  p Welcome to #{title}
  p Tirumani Shivani Goud 
  button(type='button',class='btn btn-primary' onclick="doBump()") press me
  div(id="bump") Bump the count
  script.
      var count = 0;
      var bump = 1;
      function doBump() {
          count+=bump;
          bump+=1;
          document.getElementById("bump").innerHTML=`Count ${count} with bump ${bump}`;
    }

